spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/books
    username: books
    password: books

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      base-path: /

server:
  port: 8000

---
spring:
  profiles: test
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/books_test
